[project]
name = "audio-parquet-merger"
version = "0.1.0"
description = "Merge two parquet files into one to compare signals"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "polars>=1.34.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.0",
]
